<div class="row">

  <div class="col-3">
    <h1 class="title mypage">マイページ</h1>
    <ul class="nav nav-sidebar mt-5">
      <li><%= fa_icon("pencil lg") %> <%= link_to "ユーザー情報を編集する", edit_user_registration_path, class: "text-dark ml-2" %></li>
      <li><%= fa_icon("comment lg") %> <%= link_to "口コミを投稿する", new_post_path, class: "text-dark ml-2" %></li>
      <li><%= fa_icon("heart lg") %> <%= link_to "いいねした口コミ", likes_user_path(current_user), class: "text-dark ml-2" %></li>
    </ul>
  </div>

  <div class="col-9">
    <h2 class="title">自分が投稿した口コミ</h2>
      <% if @post %>
          <div class="card mt-5">
            <div class="card-header">
              <%= @post.university %>&nbsp;<%= @post.department %>&nbsp;<%= @post.major %>&nbsp;<%= @post.professor %>研究室(ゼミ)の口コミ
            </div>
            <div class="card-body">
              <p class="card-text"><%= @post.detail %></p>
              <p class="date"><%= @post.created_at.strftime('%Y年%m月%d日') %>投稿</p>
              <%= link_to "編集する", edit_post_path(@post), class: "btn btn-primary" %>
              <%= link_to "削除する", @post, method: :delete, data: { confirm: "本当に削除しますか?" }, class: "btn btn-secondary" %>
            </div>
          </div>
      <% else%>
        <div class="card mt-5">
          <div class="card-body text-center">
            投稿した口コミはありません。
          </div>
        </div>
      <% end %>
  </div>

</div>

